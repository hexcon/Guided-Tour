# Guided-Tour Workflow

Canonical routing, session detection, phase transitions, and artifact structure live in `SKILL.md`. Read that file.

This file stays as the named overview that `README.md` points to. It does not restate the routing rules, so the two copies cannot drift apart.
